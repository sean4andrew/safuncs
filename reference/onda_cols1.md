# Primary ONDA Color Vector

A vector containing the hex codes for the primary theme of colors for
ONDA: c("#004750", "#006B68", "#65BBB0").

## Usage

``` r
data(onda_cols1)
print(onda_cols1)
```

## Format

An object of class `character` of length 3.
