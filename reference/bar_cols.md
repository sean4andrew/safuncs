# Barplot Color Vector

A six-color vector for barplots, containing the hex codes: c("black",
"#41afaa", "#466eb4", "#00a0e1", "#e6a532", "#d7642c").

## Usage

``` r
data(bar_cols)
print(bar_cols)
```

## Format

An object of class `character` of length 6.
